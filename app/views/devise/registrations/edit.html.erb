<h2>Modifier le profil  <%= resource_name.to_s.humanize %></h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
<%= render 'shared/devise_errors' %>

  <div class="form-group">
    <%= f.text_field :fullname, autofocus: true, class: "form-control", placeholder: "Nom complet ( Prénom + Nom)" %>
  </div>


  <div class="form-group">
    <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "Adresse e-mail" %>
  </div>



  <div class="form-group">

       <span class="button ui circular mini button basic green">

            Upload ma photo

            <%= f.file_field :avatar %>

       </span>  </div>

  <div class="form-group">
   <%= f.text_field :phone_number, class: "form-control", placeholder: "Numéro de téléphone" %>
   </div>
  <div class="form-group">

   <%= f.text_area :description, rows:5, col:25, class: "form-control", placeholder: "Ma description" %>

   </div>


  <div class="form-group">
    <%= f.password_field :password, autocomplete: "off", placeholder: "Nouveau mot de passe (si vous souhaitez changer de mot de passe)"  %>
    <% if @minimum_password_length %>
      <br />
      <em><%= @minimum_password_length %> characters minimum</em>
    <% end %>
  </div>

  <div class="form-group">
    <%= f.password_field :password_confirmation, autocomplete: "off", placeholder: "Confirmation du mot de passe" %>
  </div>

  <div class="form-group">
<%= f.password_field :current_password, autocomplete: "off", class: "form-control", placeholder: "Mot de passe actuel (pour confirmer tout changement)" %>
  </div>


  <div class="actions">
    <%= f.submit "Enregistrer" %>
  </div>
<% end %>

<h3>Supprimer Mon Compte</h3>

<p>Déjà? Vous allez nous manquer... <%= button_to "SUPPRIMER", registration_path(resource_name), data: { confirm: "Cette action est définitive, vous confirmer la suppression du compte?" }, method: :delete %></p>

<%= link_to "Back", :back %>
